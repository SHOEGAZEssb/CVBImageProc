<UserControl x:Class="CVBImageProc.Processing.Automation.ProcessorAutomationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CVBImageProc.Processing.Automation"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="3"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <ListBox Grid.Row="0" ItemsSource="{Binding AllAutomations}" DisplayMemberPath="PropertyName" SelectedItem="{Binding SelectedAutomation}"/>
    <ContentControl Grid.Row="2" Content="{Binding SelectedAutomation}">
      <ContentControl.Resources>
        <DataTemplate DataType="{x:Type local:BoolPropertyAutomationViewModel}">
          <local:BoolPropertyAutomationView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:IntPropertyAutomationViewModel}">
          <local:IntPropertyAutomationView/>
        </DataTemplate>
      </ContentControl.Resources>
    </ContentControl>
  </Grid>
</UserControl>
